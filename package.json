{
  "name": "make-my-picture",
  "version": "0.0.1",
  "description": "grandad says make my picture",
  "main": "index.js",
  "scripts": {
    "start": "browser-sync start --server --watch --browser \"chrome\"",
    "build": "copyfiles \"**/*\" -e 'node_modules/**/*' -e 'dist/**/*' dist"
  },
  "author": "dp",
  "license": "MIT",
  "devDependencies": {
    "copyfiles": "^2.4.1"
  }
}
